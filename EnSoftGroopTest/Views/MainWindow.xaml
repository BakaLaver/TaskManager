<Window x:Class="EnSoftGroopTest.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:EnSoftGroopTest.Converters"
        xmlns:local="clr-namespace:EnSoftGroopTest"
        mc:Ignorable="d"
        Title="Task Manager" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <converters:PriorityToColorConverter x:Key="PriorityToColorConverter"/>
        <converters:DueDateToBrushConverter x:Key="DueDateToBrushConverter"/>
        <converters:StatusToBooleanConverter x:Key="StatusToBooleanConverter"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель с кнопками -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="➕ Добавить" Width="100" Height="30" Margin="0,0,5,0" 
                    Command="{Binding AddTaskCommand}"/>
            <Button Content="✏️ Редактировать" Width="100" Height="30" Margin="0,0,5,0" 
                    Command="{Binding EditTaskCommand}"/>
            <Button Content="🗑️ Удалить" Width="100" Height="30" Margin="0,0,5,0" 
                    Command="{Binding DeleteTaskCommand}"/>
        </StackPanel>

        <!-- Панель фильтрации и поиска -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Фильтр:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox Width="120" Height="30" Margin="0,0,10,0" 
                      SelectedItem="{Binding SelectedFilter}">
                <ComboBoxItem Content="Все" IsSelected="True"/>
                <ComboBoxItem Content="Активные"/>
                <ComboBoxItem Content="Завершённые"/>
            </ComboBox>

            <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Width="200" Height="30" 
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     VerticalContentAlignment="Center"/>
        </StackPanel>

        <!-- Список задач -->
        <ListView Grid.Row="2" 
                  ItemsSource="{Binding TasksView}"
                  SelectedItem="{Binding SelectedTask}">

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Background" Value="{Binding DueDate, Converter={StaticResource DueDateToBrushConverter}}"/>
                    <Setter Property="ContextMenu">
                        <Setter.Value>
                            <ContextMenu>
                                <MenuItem Header="Выполнено / Активно" 
                                          Command="{Binding DataContext.ToggleStatusCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                          CommandParameter="{Binding}"/>
                                <MenuItem Header="Удалить" 
                                          Command="{Binding DataContext.DeleteTaskCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                            </ContextMenu>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Status}" Value="Completed">
                            <Setter Property="Opacity" Value="0.6"/>
                            <Setter Property="FontStyle" Value="Italic"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.View>
                <GridView>
                    <GridViewColumn Header="✓" Width="30">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding Status, Converter={StaticResource StatusToBooleanConverter}}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Название" Width="200" 
                                   DisplayMemberBinding="{Binding Title}"/>

                    <GridViewColumn Header="Описание" Width="250" 
                                   DisplayMemberBinding="{Binding Description}"/>

                    <GridViewColumn Header="Приоритет" Width="80">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Priority}" 
                                          Foreground="{Binding Priority, Converter={StaticResource PriorityToColorConverter}}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Срок" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding DueDate, StringFormat=\{0:dd.MM.yyyy\}}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Создана" Width="100" 
                                   DisplayMemberBinding="{Binding CreatedAt, StringFormat=\{0:dd.MM.yyyy\}}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
